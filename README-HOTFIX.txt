WAT CHALO AR CAMERA HOTFIX

Replace only app.js in your GitHub repository.

This version:
- opens the camera before loading the AI model;
- uses CPU mode for broader phone compatibility;
- shows each loading stage;
- stops with a visible error instead of loading forever.

After committing app.js, wait for Netlify to say Published, then open:
https://bang-kruai-tour-ar.netlify.app/?v=hotfix1
